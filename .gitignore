# Secrets — never commit
.env

# Dev-only launchers (not for distribution)
dev.py
Start - Dev (Mac).command

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
venv/
.venv/

# App-generated output (user data now lives in ~/Library/Application Support/APK-JTM/)
reports/
reports.migrated/
uploads/
mobsf_raw.json
report_*.html
report_*.md

# Legacy migration artefacts
.env.migrated

# APK files
*.apk

# IDE / local tooling
.claude/
.DS_Store

# Root-level logo duplicate (canonical copy is static/logo.png)
/logo.png
